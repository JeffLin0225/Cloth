package com.ispan.demo.model;

import java.util.Objects;

import jakarta.persistence.Entity;
import lombok.Getter;
import lombok.NoArgsConstructor;
import lombok.Setter;

@Getter
@Setter
@NoArgsConstructor
@Entity
public class CustomerserviceId {

	private Users users_id;
	
	@Override
	public int hashCode() {
		return Objects.hash(admin_id, users_id);
	}

	@Override
	public boolean equals(Object obj) {
		if (this == obj)
			return true;
		if (obj == null)
			return false;
		if (getClass() != obj.getClass())
			return false;
		CustomerserviceId other = (CustomerserviceId) obj;
		return Objects.equals(admin_id, other.admin_id) && Objects.equals(users_id, other.users_id);
	}

	private Admin admin_id;

}
